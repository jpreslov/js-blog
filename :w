<script>
	import '../app.css';
	let username;
	let password;

	const handleCredSignin = () => {
		console.log(username, password);
	};
</script>

<div class="flex flex-row justify-center rounded-md outline-black p-3 m-2 bg-red-400">
	<div class="flex flex-col">
		<h1 class="text-xl">Signin</h1>
			<button
				on:click={() => handleCredSignin()}
				class="mx-4 h-10 rounded-full bg-blue-500 px-4 py-2 font-bold text-white hover:bg-blue-700"
				>Sign in</button
			>
			<span>
				<p>OR</p>
			</span>
			<button>Sign in with GitHub</button>
	</div>
</div>
